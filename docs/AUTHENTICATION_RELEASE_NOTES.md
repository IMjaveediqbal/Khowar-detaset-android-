# Authentication Release Notes

This branch adds a Firebase-authenticated application gate, email/password authentication, Google Credential Manager sign-in, password reset, session persistence, Firebase-UID local profiles, and trusted server RBAC refresh. New public accounts are always contributors; privileged roles are never self-selected.
